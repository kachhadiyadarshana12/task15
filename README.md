pd.read_csv() stores data in a dataframe, easy manipulation and analysis.
head()->rows shows, info()->column name datatypes null values, describe() -> statistical summary like mean, min,max.
isnull()->missing values, fillna()->replace missing values with mean.
filtering is select rows based conditions, arrange data in ascending.
groupby(), mean(), sum(), count() count perfectly.
new columns are create for bonus and total compensation.
to_csv(cleanded_data.csv) perpose store processed data for future use.
